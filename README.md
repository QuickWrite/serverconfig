# Server Configurations
These are my server configurations for my very simple web server.

## Setup
To set up the server configuration, first clone this repository into your desired folder.

Then bootstrap all the necessary resources by
```cmd
sudo bash scripts/bootstrap.sh
```
